[project]
name = "kernel-simulator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ruff>=0.14.13",
]

[tool.ruff]
exclude = [
  ".git",
  ".venv"
]

line-length = 88
indent-width = 4
